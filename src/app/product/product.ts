export interface Product {
    id: number;       // Unique identifier for the product (typically numeric)
    name: string;     // Name of the product (typically a string)
    price: number;    // Price of the product (numeric value)
    category: string; // Category of the product (string describing the category)
}
